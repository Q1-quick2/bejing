package com.sutong.bjstjh.hcp.tools;

//import com.amazonaws.AmazonClientException;
//import com.amazonaws.AmazonServiceException;
//import com.amazonaws.ClientConfiguration;
//import com.amazonaws.Protocol;
//import com.amazonaws.auth.profile.ProfileCredentialsProvider;
//import com.amazonaws.services.s3.model.HeadBucketResult;

public class ListKeys {
	/*
	 * private static String bucketName = "testbk"; public static void main(String[] args) throws
	 * IOException { ClientConfiguration config = new ClientConfiguration();
	 * //config.setSignerOverride("AWS4SignerType"); config.setProtocol(Protocol.HTTP); AmazonS3
	 * s3client = new AmazonS3Client(new ProfileCredentialsProvider(),config);
	 * s3client.setEndpoint("gic.bgp.cnbj01.cdsgss.com"); try { HeadBucketRequest req = new
	 * HeadBucketRequest(bucketName); HeadBucketResult res = s3client.headBucket(req); if(res!=null)
	 * System.out.println("true"); } catch (AmazonServiceException ase) { ase.printStackTrace();
	 * System.out.println("Caught an AmazonServiceException, " + "which means your request made it " +
	 * "to Amazon S3, but was rejected with an error response " + "for some reason.");
	 * System.out.println("Error Message: " + ase.getMessage()); System.out.println("HTTP Status Code: "
	 * + ase.getStatusCode()); System.out.println("AWS Error Code: " + ase.getErrorCode());
	 * System.out.println("Error Type: " + ase.getErrorType()); System.out.println("Request ID: " +
	 * ase.getRequestId()); } catch (AmazonClientException ace) {
	 * System.out.println("Caught an AmazonClientException, " + "which means the client encountered " +
	 * "an internal error while trying to communicate" + " with S3, " +
	 * "such as not being able to access the network."); System.out.println("Error Message: " +
	 * ace.getMessage()); } }
	 */
}
